# Seguimiento de pruebas unitarias de componentes

## Completado
- [x] `CustomSwitcher` (renderizado, selección de opciones y cierre del desplegable)
- [x] `LanguageSwitcher` (integración con i18n y traspaso de propiedades)
- [x] `TextEllipsis` (detección de overflow, modo report y configuración de tooltip)
- [x] `Avatar` (icónico, imágenes externas y URLs firmadas con store y CDN)
- [x] `Search` (filtros por teclado, eliminación, rango de fechas y módulos auxiliares)
- [x] `Button` (traducciones, permisos y variantes redondeadas/icono)
- [x] `Dropdown` (despliegue, selección y sincronización controlada)
- [x] `Card` (estilos por defecto y combinaciones de flags decorativos)

## Pendiente
- [ ] `Modal` (expansión, cierre manual y slots de cabecera/pie)
- [ ] Revisión de componentes restantes sin cobertura específica
