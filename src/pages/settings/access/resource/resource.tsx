import { type FunctionComponent } from 'preact';
import { Section } from '@/components/common/section/section';

export const ResourceSettingPage: FunctionComponent = () => {
  return <Section className='pt-2'>RESOURCES</Section>;
};
